# PRD - Step 1: 냉장고 이미지 재료 인식

## 1. 개요
사용자가 냉장고 내부 사진을 업로드하면, 비전 모델을 이용해 사진 속 식재료를 인식하고 목록화한다. 이 단계의 산출물(재료 목록)은 Step 2 레시피 생성의 입력으로 사용된다.

## 2. 목표
- 사용자가 이미지를 업로드하면 재료명 목록을 텍스트로 반환한다.
- 별도 로그인/저장 없이 단독으로 동작 및 테스트 가능해야 한다 (Step 3에서 저장 기능 연결).

## 3. 사용할 모델
- `google/gemma-3-27b-it:free` (OpenRouter)
- **알려진 리스크**: 사전 테스트에서 이 슬러그가 "This model is unavailable for free. use paid slug: google/gemma-3-27b-it" 404 오류를 반환한 이력이 있음. OpenRouter의 무료 모델 가용성은 수시로 바뀌므로, 구현 시:
  1. 호출 전/배포 전 `GET https://openrouter.ai/api/v1/models`로 해당 슬러그의 현재 가용 여부를 확인한다.
  2. 404/429 발생 시 대체 비전 모델(예: `nvidia/nemotron-nano-12b-v2-vl:free`)로 자동 폴백하는 로직을 둔다.

## 4. 사용자 흐름
1. 사용자가 웹 페이지에서 냉장고 사진 파일을 선택/업로드한다.
2. 업로드된 이미지가 서버로 전송된다.
3. 서버가 이미지를 base64로 인코딩해 OpenRouter API에 전달한다.
4. 모델이 반환한 텍스트에서 재료명 목록을 추출/파싱한다.
5. 프론트엔드에 재료 목록을 표시한다 (사용자가 확인 후 수정 가능).

## 5. 기능 요구사항
- 이미지 업로드: jpg/png, 최대 10MB.
- 프롬프트: 모델에게 "이미지 속 식재료를 한국어 명사 목록으로만 답하라"는 형태의 구조화된 응답을 요청한다 (예: JSON 배열 형식 지정).
- 응답 파싱: 모델 응답이 자유 텍스트일 경우에도 재료 목록만 안전하게 추출하는 파싱 로직 필요 (JSON 파싱 실패 시 줄바꿈/쉼표 기준 fallback 파싱).
- 재료 목록 수동 편집: 사용자가 인식 결과에서 항목을 추가/삭제할 수 있어야 한다.

## 6. API 스펙 (초안)
```
POST /api/analyze-image
Request: multipart/form-data { image: file }
Response 200: { "ingredients": ["계란", "대파", "두부", ...] }
Response 4xx/5xx: { "error": "메시지" }
```

## 7. 비기능 요구사항
- 이미지 1장당 응답 시간 10초 이내 목표 (모델 응답 지연 고려).
- OpenRouter API 키는 서버 환경변수(`OPENROUTER_API_KEY`)로만 접근, 프론트엔드에 노출 금지.
- 모델 오류(404/429/5xx) 발생 시 사용자에게 재시도 안내 메시지 표시.

## 8. 완료 기준 (Acceptance Criteria)
- 테스트 이미지(예: 기존 `img/냉장고.jpg`, `img/냉장고2.jpg`) 업로드 시 합리적인 재료 목록이 반환된다.
- 모델 호출 실패 시에도 앱이 크래시 없이 에러 메시지를 표시한다.
- 재료 목록이 Step 2가 바로 사용할 수 있는 배열(JSON) 형태로 저장/전달된다.

## 9. 제외 범위 (Out of Scope)
- 레시피 생성 (Step 2에서 처리)
- 사용자 계정/저장 기능 (Step 3에서 처리)
python, flask