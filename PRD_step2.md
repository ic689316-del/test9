# PRD - Step 2: 재료 기반 레시피 생성

## 1. 개요
Step 1에서 인식된 재료 목록을 입력받아, 텍스트 생성 모델을 이용해 실제로 만들 수 있는 레시피를 추천한다.

## 2. 목표
- 재료 목록(JSON 배열)을 입력하면 레시피 후보 여러 개를 구조화된 형태로 반환한다.
- Step 1과 독립적으로 재료 목록만 있으면 단독 테스트 가능해야 한다 (수동 입력으로도 동작).

## 3. 사용할 모델
- `deepseek/deepseek-chat-v3.1:free` (OpenRouter)
- Step 1과 마찬가지로 무료 모델 가용성이 변동될 수 있으므로, 호출 실패(404/429) 시 폴백 모델을 둘지 여부를 초기 개발 단계에서 결정한다.

## 4. 사용자 흐름
1. Step 1에서 넘어온 재료 목록(또는 사용자가 직접 편집한 목록)을 확인한다.
2. "레시피 추천받기" 버튼을 누른다.
3. 서버가 재료 목록을 프롬프트에 담아 모델에 요청한다.
4. 모델이 레시피 후보(예: 3개)를 제목/재료/조리순서/조리시간/난이도 포함한 구조로 응답한다.
5. 프론트엔드가 레시피 카드 형태로 결과를 표시한다.

## 5. 기능 요구사항
- 프롬프트 설계: 모델에게 "보유 재료만으로/최소한의 추가 재료로 만들 수 있는 레시피 N개를 JSON으로 반환"하도록 지시.
- 응답 스키마 고정: `title`, `usedIngredients`, `extraIngredients`(부족한 재료), `steps`(배열), `cookTimeMinutes`, `difficulty`.
- 재료가 부족한 레시피의 경우 "추가로 필요한 재료"를 별도로 표시한다.
- 레시피 개수: 기본 3개, UI에서 재생성(새로고침) 가능.
- JSON 파싱 실패 시 원문 텍스트를 그대로 노출하는 fallback 처리.

## 6. API 스펙 (초안)
```
POST /api/generate-recipes
Request: { "ingredients": ["계란", "대파", "두부"] }
Response 200: {
  "recipes": [
    {
      "title": "두부 계란찜",
      "usedIngredients": ["계란", "두부"],
      "extraIngredients": ["소금", "물"],
      "steps": ["...", "..."],
      "cookTimeMinutes": 15,
      "difficulty": "쉬움"
    }
  ]
}
Response 4xx/5xx: { "error": "메시지" }
```

## 7. 비기능 요구사항
- 동일 재료 조합에 대한 반복 호출 비용을 줄이기 위한 캐싱(선택 사항, 예: 최근 요청 결과 메모리 캐시).
- 응답 시간 15초 이내 목표.
- API 키는 서버 사이드에서만 사용 (Step 1과 동일 정책).

## 8. 완료 기준 (Acceptance Criteria)
- Step 1에서 생성된 재료 목록을 그대로 입력하면 오류 없이 레시피 목록이 반환된다.
- 각 레시피가 정의된 스키마(JSON)를 만족한다.
- 모델 오류 시에도 앱이 에러 메시지를 표시하고 재시도 가능하다.

## 9. 제외 범위 (Out of Scope)
- 이미지 인식 (Step 1에서 처리)
- 레시피 저장/사용자 프로필 (Step 3에서 처리)
python, flask